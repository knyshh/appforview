import styled from "styled-components";

const PatientsPage = styled.div`
  padding: 90px 0 20px 0;
`;

const Amount = styled.div`
  font-family: var(--fontAvenirHeavy);
  font-size: 16px;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.38;
  letter-spacing: normal;
  color: #0091ff;
  padding-left: 28px;
  text-align: left;
`;



export default {
  PatientsPage,
  Amount
};
