import React from 'react';

import UpdatePasswordModal from "components/UpdatePassword/UpdatePassword.Modal";

const UpdatePassword = () => {
    return (
       <UpdatePasswordModal/>
    );
};

export default UpdatePassword;