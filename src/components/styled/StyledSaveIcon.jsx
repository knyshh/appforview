import styled from 'styled-components';
import SaveIcon from '../../assets/img/save.svg';

const StyledSaveIcon = styled.i`
	  	display: inline-block;
	  	width: 24px;
	  	height: 24px;
	  	background: url(${SaveIcon}) center center no-repeat;
	  	background-size: contain;
	  	margin-right: 14px;
`;

export default StyledSaveIcon;
